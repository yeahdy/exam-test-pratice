server:
  port: 8761

spring:
  application:
    name: discoveryservice
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: bookmark
  profiles:
    active: dev

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
